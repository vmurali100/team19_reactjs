const initialData = {
    users : ["Seetha", "Geetha"],
    products : [],
    books : []
};

export const reducer = (state = initialData, action) =>{
    // switch(action.payLoad) {
    //     default:
    //         return state
    // }
    return state
}