const initialData = {
  users: ["Ram","Ravi"],
  products:[],
  books:[]
};
export const rootReucer = (state = initialData, action) => {
  switch (action.payLoad) {
    default:
        return state
  }
};
